{
  "name": "HEASoft Analysis Environment",
  // "image": "heasoft:v6.36",
  "runArgs": [
    "--net=host",
    "--rm"
  ],
  "build": {
    "dockerfile": "Dockerfile"
  },
  "containerEnv": {
    "CALDB": "/home/heasoft/CALDB",
    "GEOMAG_PATH": "/home/heasoft/CALDB/data/gen/pcf/geomag",
    "DISPLAY": "${localEnv:DISPLAY}"
  },
  "mounts": [
    // 1. CALDBのマウント
    "source=/home/isseikurihara/HEASoft/CALDB,target=/home/heasoft/CALDB,type=bind",
    
    // 2. 重たい観測データのマウント (Git管理外の安全地帯)
    // ここがコンテナ内の /home/heasoft/data になります
    "source=/home/isseikurihara/HEASoft/GRB221009A/data_NICER,target=/home/heasoft/data,type=bind"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "eamodio.gitlens"  // Git管理を便利にする拡張機能(おすすめ)
      ]
    }
  },
  // 起動時の初期設定
  "postCreateCommand": "echo 'export CALDB=/home/heasoft/CALDB' >> ~/.bashrc && echo 'export GEOMAG_PATH=/home/heasoft/CALDB/data/gen/pcf/geomag' >> ~/.bashrc"
}
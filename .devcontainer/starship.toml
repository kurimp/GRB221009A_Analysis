# モジュールの表示順序
format = """
[┌───────────────────>](bold green)
[│](bold green) $directory$git_branch$git_metrics$git_status$python$memory_usage$cmd_duration
[└─>](bold green) $character"""

# 入力行のアイコン
[character]
success_symbol = "[⚡](bold green)"
error_symbol = "[✖](bold red)"

# ディレクトリ (パス)
[directory]
truncation_length = 3
truncation_symbol = "…/"
style = "bold cyan"

# Git関連
[git_branch]
symbol = " "
style = "bold purple"

[git_status]
style = "bold red"
format = '[$all_status$ahead_behind]($style) '

[git_metrics]
disabled = false
added_style = "bold blue"
deleted_style = "bold red"

# Python環境
[python]
symbol = "🐍"
style = "bold yellow"
format = '[${symbol}${version} (\($virtualenv\))]($style) '

# メモリ (解析用)
[memory_usage]
disabled = false
threshold = 80
symbol = "🔥"
format = '$symbol[${ram_pct}%]($style) '
style = "bold red"

# コマンド実行時間
[cmd_duration]
min_time = 2000
format = "⏱ [$duration]($style) "
style = "bold yellow"